package com.narde.gestionaleosteopatabetto.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Custom color palette inspired by teal/turquoise medical theme
 * These colors create a calming, professional atmosphere suitable for healthcare applications
 */

// Primary teal colors - main brand colors
val TealPrimary = Color(0xFF1BA3B8) // Main teal color from the image
val TealPrimaryDark = Color(0xFF0F7A8A) // Darker shade for contrast
val TealPrimaryLight = Color(0xFF4DB8C7) // Lighter shade for backgrounds

// Secondary teal colors - supporting colors
val TealSecondary = Color(0xFF2CBAD3) // Brighter teal for accents
val TealSecondaryDark = Color(0xFF1A8A9D) // Darker secondary
val TealSecondaryLight = Color(0xFF7DCED9) // Light secondary for surfaces

// Surface and background colors
val TealSurface = Color(0xFFF0FAFB) // Very light teal for surfaces
val TealSurfaceVariant = Color(0xFFE6F7F9) // Slightly darker surface variant
val TealBackground = Color(0xFFFAFEFE) // Almost white with teal hint

// On-color variants (text colors on colored backgrounds)
val OnTealPrimary = Color(0xFFFFFFFF) // White text on primary teal
val OnTealSecondary = Color(0xFFFFFFFF) // White text on secondary teal
val OnTealSurface = Color(0xFF1A1C1E) // Dark text on light surfaces
val OnTealBackground = Color(0xFF1A1C1E) // Dark text on background

// Error colors - complementary to teal
val TealError = Color(0xFFBA1A1A) // Standard error red
val TealOnError = Color(0xFFFFFFFF) // White text on error
val TealErrorContainer = Color(0xFFFFDAD6) // Light error background
val TealOnErrorContainer = Color(0xFF410002) // Dark text on error container

// Outline and variant colors
val TealOutline = Color(0xFF6F797A) // Neutral outline color
val TealOutlineVariant = Color(0xFFBFC8C9) // Lighter outline variant
val TealScrim = Color(0xFF000000) // Black scrim for overlays

// Additional utility colors
val TealInverseSurface = Color(0xFF2F3132) // Dark surface for inverse theme
val TealInverseOnSurface = Color(0xFFF0F1F1) // Light text on dark surface
val TealInversePrimary = Color(0xFF7DCED9) // Light primary for dark theme